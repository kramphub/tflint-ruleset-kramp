# This should pass without error... if the 'ignores' are respected by tflint (that's what being verified with this configuration)
# See the test_*.tf files in this directory for the test configuration.
